# 项目架构设计图

> 本文档展示小i跑腿平台的系统架构设计，涵盖整体三层架构、后端 Maven 模块结构、管理端与移动端前端架构。

---

## 系统整体架构图

> 三层架构：展示层 → 业务层 → 数据层的完整调用链路与跨切面基础设施

```mermaid
flowchart TD
    subgraph PRESENTATION["展示层 (Presentation)"]
        direction LR
        MOBILE["📱 移动端<br/>uni-app 微信小程序<br/>34个页面 · 5 Tab"]
        ADMIN["🖥️ 管理端<br/>Vue 3 + Element Plus<br/>15个视图"]
    end

    GATEWAY["API 网关层 (Nginx)<br/>路由分发 · 静态资源 · WebSocket 代理<br/>端口 80/443 → 后端 8080"]

    subgraph BUSINESS["业务层 (Business) · Spring Boot 3.2.0"]
        direction TB

        subgraph CONTROLLER["Controller 控制层 (15个)"]
            direction LR
            CTRL_ADMIN["admin 包<br/>AdminController<br/>UserManageController..."]
            CTRL_USER["user 包<br/>UserController<br/>TaskController..."]
            CTRL_COMMON["通用接口<br/>CommonController<br/>文件上传/下载"]
        end

        INTERCEPTOR["拦截器层<br/>JwtTokenAdminInterceptor (token 头)<br/>JwtTokenUserInterceptor (authentication 头)"]

        subgraph SERVICE["Service 服务层 (20+个)"]
            direction LR
            SVC_CORE["核心业务<br/>UserServiceImpl/TaskServiceImpl/TaskOrderServiceImpl/RunnerProfileServiceImpl/PaymentServiceImpl"]
            SVC_SUPPORT["支撑业务<br/>ChatMessageServiceImpl/ReviewServiceImpl/NotificationServiceImpl/TransactionServiceImpl/AddressServiceImpl/CreditLogServiceImpl"]
            SVC_ADMIN["管理业务<br/>AdminDashboardServiceImpl/OperationLogServiceImpl/SystemConfigServiceImpl/AdminServiceImpl"]
        end

        AOP["AOP 切面层<br/>@OperationLog 操作日志记录<br/>@SendNotification 通知推送<br/>@RedisDefend 防缓存穿透/击穿"]

        MAPPER["Mapper 数据访问层 (15个)<br/>MyBatis-Plus BaseMapper<br/>LambdaWrapper 参数化查询"]
    end

    subgraph DATA["数据层 (Data)"]
        direction LR
        MYSQL[("MySQL 8<br/>runningerrands<br/>15张表")]
        REDIS[("Redis 7<br/>缓存 · 分布式锁<br/>登录保护 · 验证码")]
        OSS["阿里云 OSS<br/>图片/文件存储"]
    end

    subgraph CROSS["跨切面基础设施"]
        direction LR
        JWT["JWT 双令牌<br/>access(2h) + refresh(7d)"]
        REDISSON["Redisson<br/>分布式锁"]
        STOMP["STOMP WebSocket<br/>即时通讯"]
        SWAGGER["Knife4j<br/>API 文档"]
    end

    PRESENTATION -->|"HTTPS /api"| GATEWAY
    GATEWAY -->|"反向代理"| CONTROLLER
    CONTROLLER --> INTERCEPTOR
    INTERCEPTOR --> SERVICE
    SERVICE --> MAPPER
    MAPPER --> MYSQL
    MAPPER --> REDIS
    SERVICE --> OSS
    AOP -.-> CONTROLLER
    AOP -.-> SERVICE
    INTERCEPTOR -.-> JWT
    SERVICE -.-> REDISSON
    SERVICE -.-> STOMP
    CONTROLLER -.-> SWAGGER

    style PRESENTATION fill:#FFF0ED,stroke:#FF6B4A,stroke-width:3px
    style BUSINESS fill:#D4F5F0,stroke:#2EC4B6,stroke-width:3px
    style DATA fill:#EEF2FB,stroke:#5B9BD5,stroke-width:3px
    style CROSS fill:#FFF7ED,stroke:#C8925D,stroke-width:2px
    style GATEWAY fill:#F5F5F0,stroke:#8F8D88,stroke-width:2px
    style CONTROLLER fill:#FFFFFF,stroke:#D4D2CC,stroke-width:1px
    style SERVICE fill:#FFFFFF,stroke:#D4D2CC,stroke-width:1px
    style MAPPER fill:#FFFFFF,stroke:#D4D2CC,stroke-width:1px
    style INTERCEPTOR fill:#FEF3C7,stroke:#F59E0B,stroke-width:2px
    style AOP fill:#EDE9FE,stroke:#8B5CF6,stroke-width:2px
    style MOBILE fill:#FF6B4A,color:#FFFFFF
    style ADMIN fill:#FF6B4A,color:#FFFFFF
```

### 请求生命周期

```mermaid
sequenceDiagram
    participant Client as 客户端 (Mobile/Admin)
    participant Nginx as Nginx 网关
    participant Interceptor as JWT 拦截器
    participant Controller as Controller
    participant Service as Service
    participant AOP as AOP 切面
    participant Mapper as Mapper
    participant DB as MySQL/Redis

    Client->>Nginx: HTTPS /api/xxx
    Nginx->>Interceptor: 转发请求
    Interceptor->>Interceptor: 解析 Token · 校验签名 · 提取 userId
    Interceptor->>Interceptor: 写入 ThreadLocal (BaseContext)
    Interceptor->>Controller: 放行
    Controller->>Controller: @Valid 参数校验
    Controller->>Service: 调用业务方法
    Service->>AOP: @RedisDefend 防缓存穿透
    AOP->>DB: 查 Redis 缓存
    DB-->>AOP: 未命中
    AOP->>Mapper: 查数据库
    Service->>AOP: @OperationLog 记录操作
    Service->>AOP: @SendNotification 推送通知
    Mapper->>DB: MyBatis-Plus SQL
    DB-->>Mapper: 结果集
    Mapper-->>Service: Entity
    Service-->>Controller: VO/DTO
    Controller-->>Client: Result<T> JSON
```

---

## 后端 Maven 模块架构图

> Maven 多模块结构：common → model → server 的三层依赖关系

```mermaid
flowchart TD
    subgraph PARENT["runningerrands-parent (pom.xml)"]
        direction TB
        P_INFO["Group: com.ikeu<br/>Java 21 · Spring Boot 3.2.0<br/>MyBatis-Plus 3.5.5 · MySQL 8 · Redis 7"]
    end

    subgraph COMMON["runningerrands-common (共享工具模块)"]
        direction TB
        C1["constant<br/>MessageConstant · RedisConstant<br/>JwtClaimsConstant"]
        C2["context<br/>BaseContext (ThreadLocal)"]
        C3["enums<br/>TaskStatus · OrderStatus<br/>TransactionType"]
        C4["exception<br/>BusinessException<br/>GlobalExceptionHandler"]
        C5["properties<br/>JwtProperties · OssProperties<br/>AdminInitProperties"]
        C6["result<br/>Result<T> · PageResult<T>"]
        C7["utils<br/>JwtUtil · RedisDefendUtil<br/>SensitiveDataUtil"]
    end

    subgraph MODEL["runningerrands-model (数据模型模块)"]
        direction LR
        M1["entity (15个)<br/>User · Task · TaskOrder<br/>RunnerProfile · Review · ..."]
        M2["dto (29个)<br/>TaskPublishDTO · SendCodeDTO<br/>UserLoginDTO · ..."]
        M3["vo (28个)<br/>UserInfoVO · OrderDetailVO<br/>DashboardVO · ..."]
    end

    subgraph SERVER["runningerrands-server (Spring Boot 应用)"]
        direction TB
        S1["controller (21个)<br/>admin/ (11) · user/ (9) · CommonController"]
        S2["service (21个)<br/>impl/"]
        S3["mapper (15个)<br/>MyBatis-Plus BaseMapper"]
        S4["config (9个)<br/>OssConfiguration · WebMvcConfiguration<br/>PasswordConfiguration · RedisConfiguration<br/>RedissonConfiguration · WebSocketConfiguration<br/>MybatisPlusConfiguration · AdminInitializer<br/>DockerTestDataInitializer"]
        S5["interceptor (4个)<br/>JwtTokenAdminInterceptor<br/>JwtTokenUserInterceptor<br/>JwtHandshakeInterceptor<br/>AuthChannelInterceptor"]
        S6["aspect (7个)<br/>OperationLogAspect<br/>SendNotificationAspect · RedisDefendAspect<br/>CertifyAspect · CreditScoreAspect<br/>QueueWaitAspect · RoleCheckAspect<br/>RunnerStatusAspect"]
        S7["task (6个)<br/>OrderAutoCompleteChecker<br/>OrderTimeoutChecker · TaskTimeoutChecker<br/>CreditRecoveryTask<br/>NotificationCleanupTask<br/>PaymentIdempotentCleanupTask"]
        S8["annotation (5个)<br/>@OperationLog · @SendNotification<br/>@RequireRole · @RequireCertify<br/>@RedisDefend"]
        S9["handler<br/>GlobalExceptionHandler<br/>GlobalResponseHandler"]
        SA["websocket<br/>WebSocketUtil"]
        SB["resources<br/>application.yml · application-dev.yml<br/>static/ (管理端构建产物)"]
    end

    PARENT --> COMMON
    PARENT --> MODEL
    PARENT --> SERVER
    COMMON --> MODEL
    SERVER --> COMMON
    SERVER --> MODEL

    style PARENT fill:#FFF0ED,stroke:#FF6B4A,stroke-width:3px
    style COMMON fill:#D4F5F0,stroke:#2EC4B6,stroke-width:2px
    style MODEL fill:#EEF2FB,stroke:#5B9BD5,stroke-width:2px
    style SERVER fill:#FFF7ED,stroke:#C8925D,stroke-width:2px
```

### Maven 依赖关系

| 模块 | 依赖 | 职责 |
|------|------|------|
| `runningerrands-common` | 无外部模块依赖 | 通用工具：常量、枚举、异常、结果封装、JWT 工具类 |
| `runningerrands-model` | `common` | 数据模型：Entity (15)、DTO (29)、VO (28) |
| `runningerrands-server` | `common` + `model` | Spring Boot 应用：Controller、Service、Mapper、配置、拦截器 |

### Server 模块核心包结构

```
com.ikeu.server/
├── RunningErrandsApplication.java       # SpringBoot 启动类
├── controller/
│   ├── admin/                           # 管理端接口 (11个)
│   │   ├── AdminController.java         # 管理员账号
│   │   ├── AdminDashboardController.java # 仪表盘
│   │   ├── AdminRunnerController.java   # 跑腿员管理
│   │   ├── AdminTaskController.java     # 任务管理
│   │   ├── AdminOrderController.java    # 订单管理
│   │   ├── AdminUserController.java     # 用户管理 (含审核)
│   │   ├── TransactionController.java   # 资金流水
│   │   ├── NotificationController.java  # 消息管理
│   │   ├── OperationLogController.java  # 操作日志
│   │   ├── EmployeeController.java      # 员工管理
│   │   └── SettingsController.java      # 系统设置
│   ├── user/                            # 用户端接口 (9个)
│   │   ├── UserController.java          # 用户注册/登录
│   │   ├── TaskController.java          # 任务发布/浏览
│   │   ├── OrderController.java         # 订单操作
│   │   ├── TaskOrderController.java     # 订单状态
│   │   ├── ChatController.java          # 即时通讯
│   │   ├── AddressController.java       # 收货地址
│   │   ├── ReviewController.java        # 评价系统
│   │   ├── RunnerController.java        # 跑腿员功能
│   │   └── UserTransactionController.java # 充值/提现
│   └── CommonController.java            # 文件上传/通用接口
├── service/
│   └── impl/                            # 21 业务实现类
├── mapper/                              # 15 个 MyBatis-Plus Mapper
├── config/                              # 9 个配置类
├── interceptor/                         # 4 个拦截器
├── aspect/                              # 7 个 AOP 切面
├── annotation/                          # 5 个自定义注解
├── handler/                             # GlobalExceptionHandler · GlobalResponseHandler
├── util/                                # RedisDefendUtil · WebUtil
├── task/                                # 6 个定时任务
└── websocket/                           # WebSocket 工具
```

---

## 管理端前端架构设计图

> Vue 3 + TypeScript + Element Plus · Vite 构建 · 15个视图

```mermaid
flowchart TD
    subgraph ADMIN_ROOT["admin/src/ 目录结构"]
        direction TB

        subgraph AD_ENTRY["入口层"]
            direction LR
            AD_MAIN["main.ts<br/>注册 Element Plus 中文<br/>全局图标 · Pinia · Router"]
            AD_APP["App.vue<br/>根组件"]
        end

        subgraph AD_ROUTER["路由层 (router/)"]
            direction TB
            AD_ROUTER_INDEX["index.ts<br/>history /api/ · 18条路由<br/>meta.role 权限控制"]
            AD_GUARD["beforeEach 守卫<br/>无 token → /login<br/>有 token → /dashboard"]
        end

        subgraph AD_STORE["状态层 (stores/)"]
            direction LR
            AD_AUTH["auth.ts (Pinia Composition)<br/>token · adminInfo<br/>login() · logout()"]
            AD_APP_STORE["app.ts<br/>sidebarCollapsed<br/>toggleSidebar()"]
        end

        subgraph AD_VIEWS["视图层 (views/ · 13模块 · 15视图)"]
            direction TB
            AD_V1["dashboard · 仪表盘<br/>DashboardView.vue"]
            AD_V2["users · 用户管理<br/>UsersView.vue"]
            AD_V3["runners · 跑腿员管理<br/>RunnersView.vue"]
            AD_V4["tasks · 任务管理<br/>TasksView.vue"]
            AD_V5["orders · 订单管理<br/>OrdersView.vue"]
            AD_V6["transactions · 流水<br/>TransactionsView.vue"]
            AD_V7["notifications · 通知<br/>NotificationsView.vue"]
            AD_V8["logs · 操作日志<br/>OperationLogsView.vue"]
            AD_V9["employees · 员工<br/>EmployeesView.vue"]
            AD_V10["audit · 审核管理<br/>AuditView.vue"]
            AD_V11["settings · 设置<br/>SettingsView.vue"]
            AD_V12["LoginView.vue · 登录<br/>NotFoundView.vue · 404"]
        end

        subgraph AD_API["API 层 (api/ · 11模块)"]
            direction TB
            AD_API1["auth.ts · dashboard.ts"]
            AD_API2["users.ts · runners.ts<br/>tasks.ts · orders.ts"]
            AD_API3["transactions.ts · notifications.ts<br/>logs.ts · employees.ts<br/>settings.ts"]
        end

        subgraph AD_UTILS["工具层 (utils/)"]
            direction LR
            AD_UTIL1["request.ts<br/>Axios 实例 · 双 Token 刷新<br/>isRefreshing + 请求队列"]
            AD_UTIL2["constants.ts<br/>状态枚举映射"]
            AD_UTIL3["tokenStore.ts<br/>localStorage 读写"]
            AD_UTIL4["task-specs-parser.ts<br/>任务规格 JSON 解析"]
        end

        subgraph AD_STYLES["样式层"]
            AD_THEME["styles/theme.css<br/>CSS 自定义属性 · Element Plus 变量覆盖"]
        end
    end

    AD_ENTRY --> AD_ROUTER
    AD_ROUTER --> AD_STORE
    AD_STORE --> AD_VIEWS
    AD_VIEWS --> AD_API
    AD_API --> AD_UTILS
    AD_UTILS --> AD_STYLES

    style ADMIN_ROOT fill:#FAFAF8,stroke:#FF6B4A,stroke-width:3px
    style AD_ENTRY fill:#FFF0ED,stroke:#FF6B4A,stroke-width:2px
    style AD_ROUTER fill:#D4F5F0,stroke:#2EC4B6,stroke-width:2px
    style AD_STORE fill:#EEF2FB,stroke:#5B9BD5,stroke-width:2px
    style AD_VIEWS fill:#FFFFFF,stroke:#D4D2CC,stroke-width:2px
    style AD_API fill:#FFF7ED,stroke:#C8925D,stroke-width:2px
    style AD_UTILS fill:#FDE2E2,stroke:#EF4444,stroke-width:2px
    style AD_STYLES fill:#EDE9FE,stroke:#8B5CF6,stroke-width:2px
```

### 请求响应数据流

```mermaid
sequenceDiagram
    participant View as Vue 组件
    participant Store as Pinia Store
    participant API as api/ 模块
    participant Axios as request.ts (Axios)
    participant Backend as 后端 /api

    View->>API: 调用 API 方法
    API->>Axios: get/post/put/del
    Axios->>Axios: 请求拦截器: 附加 token 头
    Axios->>Backend: HTTP 请求
    Backend-->>Axios: JSON 响应 {code, data, msg}
    Axios->>Axios: 响应拦截器: 解包 (code=1 → data)
    Axios->>Axios: code≠1 → ElMessage.error
    Axios-->>API: data / PageResult
    API-->>View: 响应数据
    View->>Store: 更新状态
    Store-->>View: 响应式更新 UI

    Note over Axios,Backend: 401 时: isRefreshing + 请求队列<br/>防止并行刷新 Token 竞态
```

---

## 移动端前端架构设计图

> uni-app (Vue 3) 微信小程序 · 34个页面 · 5 Tab · STOMP WebSocket

```mermaid
flowchart TD
    subgraph MOBILE_ROOT["mobile/ 目录结构"]
        direction TB

        subgraph MB_ENTRY["入口与配置"]
            direction TB
            MB_MAIN["main.js<br/>注册全局组件 · 全局样式"]
            MB_PAGES["pages.json<br/>34 页面注册 · 5 Tab 自定义<br/>navigationStyle: custom"]
            MB_MANIFEST["manifest.json<br/>微信小程序配置"]
        end

        subgraph MB_PAGES_LAYER["页面层 (34页 · 5 Tab · 分包架构)"]
            direction TB
            subgraph MB_TAB["主包 pages/ (7页)"]
                MB_T1["index · 首页"]
                MB_T2["task-hall · 任务大厅"]
                MB_T3["orders · 订单列表"]
                MB_T4["message · 消息列表"]
                MB_T5["user-profile · 我的"]
            end
            subgraph MB_SUB["子包 (5包 · 27页) + preloadRule"]
                direction LR
                MB_PUB["pages-order (7页)<br/>order-waiting · order-success<br/>order-completed · order-delivering<br/>general-publish · cancel-reason<br/>station-select"]
                MB_ORDER["pages-service (4页)<br/>coffee-order · print-order<br/>paper-express · service-publish"]
                MB_USER["pages-user (9页)<br/>wallet · bills · runner-cert<br/>phone-edit · identity-verify<br/>password-manage · privacy-security<br/>profile-settings · certify"]
                MB_RUNNER["pages-social (5页)<br/>chat-detail · notifications<br/>runner-dashboard · runner-reviews<br/>runner-profile"]
                MB_OTHER["pages-address (2页)<br/>address-list · address-edit"]
            end
        end

        subgraph MB_API["API 层 (api/ · 11文件)"]
            direction TB
            MB_API_IDX["index.js<br/>统一命名空间导出"]
            MB_API_MOD["user · task · order · chat<br/>runner · address · transaction<br/>notification · review · common"]
        end

        subgraph MB_STORE["状态管理 (store/ · 2个模块)"]
            direction LR
            MB_STORE1["index.js (Options API)<br/>用户信息 · 16字段<br/>balance · isCertify · hasPayPassword<br/>登录/退出自动 STOMP 连接"]
            MB_STORE2["chat.js (Composition API)<br/>STOMP 全局单例<br/>消息 Map 缓存 · 乐观更新<br/>去重 · 分页历史 · 未读计数"]
        end

        subgraph MB_COMPONENT["组件层 (components/ · 9个)"]
            direction TB
            MB_C1["custom-navbar · custom-tabbar<br/>导航栏 + 毛玻璃 TabBar"]
            MB_C2["pay-password-dialog<br/>支付密码弹窗"]
            MB_C3["upload-grid<br/>图片上传网格"]
            MB_C4["task-forms<br/>通用任务表单"]
            MB_C5["FeeCard · GenderRestriction<br/>费用卡片 · 性别限制"]
            MB_C6["iconpark-icon · custom-icon<br/>图标组件"]
        end

        subgraph MB_UTILS["工具层 (utils/ · 16个模块)"]
            direction TB
            MB_U1["request.js<br/>get/post/put/del · auth 参数<br/>401 自动刷新 · isRefreshing 队列<br/>日期 formatDates · 响应解包"]
            MB_U2["stomp.js<br/>自制 STOMP 1.2 客户端<br/>uni.connectSocket<br/>心跳 10s · 连接超时 10s<br/>3次重试 + Token 刷新"]
            MB_U3["config.js<br/>动态环境检测<br/>微信 envVersion → devUrl/prodUrl"]
            MB_U4["error.js · toast.js · submit-guard.js<br/>pay-password.js · task-spec-keys.js<br/>banner-cache.js · useSmsCooldown.js<br/>useTaskSpecs.js · task-normalizer.js<br/>campus-data.js · custom-icons.js<br/>draft-save.js · constants.js"]
        end
    end

    MB_ENTRY --> MB_PAGES_LAYER
    MB_PAGES_LAYER --> MB_API
    MB_PAGES_LAYER --> MB_STORE
    MB_PAGES_LAYER --> MB_COMPONENT
    MB_API --> MB_UTILS
    MB_STORE --> MB_UTILS

    style MOBILE_ROOT fill:#FAFAF8,stroke:#FF6B4A,stroke-width:3px
    style MB_ENTRY fill:#FFF0ED,stroke:#FF6B4A,stroke-width:2px
    style MB_PAGES_LAYER fill:#FFFFFF,stroke:#D4D2CC,stroke-width:2px
    style MB_API fill:#D4F5F0,stroke:#2EC4B6,stroke-width:2px
    style MB_STORE fill:#EEF2FB,stroke:#5B9BD5,stroke-width:2px
    style MB_COMPONENT fill:#FFF7ED,stroke:#C8925D,stroke-width:2px
    style MB_UTILS fill:#FDE2E2,stroke:#EF4444,stroke-width:2px
    style MB_TAB fill:#FFF0ED,stroke:#FF6B4A
```

### 页面跳转与数据流

```mermaid
flowchart LR
    subgraph PAGE_FLOW["页面导航体系"]
        direction LR
        subgraph TAB_PAGES["TabBar 页面"]
            direction TB
            T1["🏠 首页"]
            T2["📋 大厅"]
            T3["📝 订单"]
            T4["💬 消息"]
            T5["👤 我的"]
        end

        subgraph NAV_PAGES["navigateTo 页面"]
            direction TB
            N1["发布页 (5个)"]
            N2["订单详情 (4个)"]
            N3["聊天详情"]
            N4["钱包/认证/设置"]
        end

        T1 -->|"发布/热门服务"| N1
        T2 -->|"点击任务"| N2
        T3 -->|"点击订单"| N2
        T4 -->|"点击会话"| N3
        T5 -->|"钱包/认证"| N4
    end

    subgraph STOMP_FLOW["WebSocket 实时通信"]
        direction TB
        SOCKET["uni.connectSocket"]
        STOMP_CLIENT["StompClient 实例"]
        HEARTBEAT["心跳 10s LF(\\n)"]
        SUBSCRIBE["订阅 /user/queue/messages"]
        PUSH["服务端推送 → 本地缓存"]
    end

    SOCKET --> STOMP_CLIENT --> HEARTBEAT --> SUBSCRIBE --> PUSH

    style PAGE_FLOW fill:#FAFAF8,stroke:#FF6B4A,stroke-width:2px
    style STOMP_FLOW fill:#D4F5F0,stroke:#2EC4B6,stroke-width:2px
    style STOMP_CLIENT fill:#DBEAFE,stroke:#3B82F6
    style PUSH fill:#D1FAE5,stroke:#34D399
```

### 页面分层职责

| 层级 | 文件 | 职责 |
|------|------|------|
| **页面层** | `pages/` (7) + 5 子包 (27) = 34 .vue | 页面模板 · 生命周期 · 用户交互 · 调用 API |
| **API 层** | `api/` (11个 .js) | 封装 HTTP 请求 · 统一认证头注入 · 响应解包 |
| **状态层** | `store/` (2个 .js) | 全局用户状态 · STOMP 连接管理 · 消息缓存 |
| **组件层** | `components/` (9个) | 导航栏 · TabBar · 支付密码 · 上传 · 表单 · 图标 |
| **工具层** | `utils/` (16个 .js) | HTTP 客户端 · STOMP · 错误处理 · toast · 防重复提交 |
| **配置层** | `main.js · pages.json · manifest.json` | 应用入口 · 页面注册 · 子包配置 · 预加载规则 |

---

## 技术栈全景图

```mermaid
flowchart LR
    subgraph FRONTEND["前端技术栈"]
        direction TB
        subgraph MOBILE_TECH["📱 移动端"]
            direction TB
            M1["框架: uni-app (Vue 3)"]
            M2["状态: Pinia"]
            M3["通信: 自制 STOMP 1.2"]
            M4["平台: 微信小程序"]
            M5["样式: CSS 自定义属性"]
        end
        subgraph ADMIN_TECH["🖥️ 管理端"]
            direction TB
            A1["框架: Vue 3.5 + TypeScript"]
            A2["UI: Element Plus 2.14"]
            A3["构建: Vite 8"]
            A4["图表: ECharts 6 + vue-echarts"]
            A5["动画: GSAP 3"]
            A6["状态: Pinia 3"]
        end
    end

    subgraph BACKEND["后端技术栈"]
        direction TB
        B1["语言: Java 21"]
        B2["框架: Spring Boot 3.2.0"]
        B3["ORM: MyBatis-Plus 3.5.5"]
        B4["数据库: MySQL 8"]
        B5["缓存: Redis 7 + Redisson"]
        B6["认证: JWT 双令牌"]
        B7["文档: Knife4j + springdoc"]
        B8["构建: Maven Wrapper"]
    end

    subgraph DEPLOY["部署与运维"]
        direction TB
        D1["容器: Docker + Docker Compose"]
        D2["网关: Nginx"]
        D3["存储: 阿里云 OSS"]
        D4["CI/CD: GitHub Actions"]
    end

    FRONTEND --> BACKEND --> DEPLOY

    style FRONTEND fill:#FFF0ED,stroke:#FF6B4A,stroke-width:3px
    style BACKEND fill:#D4F5F0,stroke:#2EC4B6,stroke-width:3px
    style DEPLOY fill:#EEF2FB,stroke:#5B9BD5,stroke-width:3px
    style MOBILE_TECH fill:#FF6B4A,color:#FFFFFF,stroke:#FF6B4A
    style ADMIN_TECH fill:#FF6B4A,color:#FFFFFF,stroke:#FF6B4A
```

---

> **文档说明**：以上架构设计图基于项目实际代码结构（`backend/` Maven 多模块、`admin/src/` Vue 3 目录、`mobile/` uni-app 目录）绘制。所有 Mermaid 图表可在支持 Mermaid 渲染的 Markdown 阅读器中直接预览。
